services:
  leadtrack:
    build: .
    ports:
      - "4000:4000"
    volumes:
      - ./server/data:/app/server/data
    environment:
      - NODE_ENV=production
      - PORT=4000
    restart: always
